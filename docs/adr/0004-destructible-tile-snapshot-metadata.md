# Destructible Tile Snapshot Metadata

Atoms match snapshots store Destructible Tile Hit Points in version 3 metadata instead of extending the human-editable ASCII board tokens. The board rows continue to describe only cell occupancy, while `destructibleTiles` records the positions and Hit Points of Tiles that also behave like normal playable Tiles.
